export interface dataStatusProps {
  icon: string,
  title: string,
  value: number,
  textColor: string,
  progressColor: string,
  cardColor: string
}

export interface perCircularProps {
  value: number,
  textColor: string,
  progressColor: string,
}

export interface dataCarProps {
  recommended: number,
  title: string,
  image: string,
  value: number,
  price: number,
  colorBg: string
}

export interface dataBookingProps {
  name: string,
  kind: string,
  image: string,
  passengers: number,
  price: number
}